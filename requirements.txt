# Pinned to the versions used for the analysis (Python 3.14).
# Install with:  pip install -r requirements.txt

# --- data fetch (main.py) ---
requests==2.34.2

# --- analysis & figures (analyze.py) ---
pandas==3.0.3
numpy==2.4.6
matplotlib==3.10.9
rdrobust==2.0.0
rddensity==3.0

# transitive but pinned for reproducibility
scipy==1.17.1
statsmodels==0.14.6
